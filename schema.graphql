type Todo {
    id: ID!
    title: String!
    content: String
    completed: Boolean!
    list: List
}

type List {
    id: ID!
    title: String!
    todos: [Todo!]!
}

type Query {
    todos:[Todo!]!
    todo(id: ID!):Todo
    lists: [List!]!
    list(id: ID!): List
}